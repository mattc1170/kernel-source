From 9cea52e5b360fc7c46c8eb78384c757df47cdec4 Mon Sep 17 00:00:00 2001
From: Hong Liu <hong.liu@intel.com>
Date: Fri, 3 Jun 2016 17:21:21 +0800
Subject: [PATCH 129/143] [VPG]: drm/i915: fix compiler warning in gen6_rps_idle

Signed-off-by: Hong Liu <hong.liu@intel.com>
---
 drivers/gpu/drm/i915/intel_pm.c |    5 ++++-
 1 files changed, 4 insertions(+), 1 deletions(-)

Index: linux-4.4/drivers/gpu/drm/i915/intel_pm.c
===================================================================
--- linux-4.4.orig/drivers/gpu/drm/i915/intel_pm.c
+++ linux-4.4/drivers/gpu/drm/i915/intel_pm.c
@@ -4522,7 +4522,10 @@ void gen6_rps_idle(struct drm_i915_priva
 
 	mutex_lock(&dev_priv->rps.hw_lock);
 	if (dev_priv->rps.enabled) {
-		intel_set_rps(dev_priv->dev, dev_priv->rps.idle_freq);
+		if (IS_VALLEYVIEW(dev))
+			vlv_set_rps_idle(dev_priv);
+		else
+			gen6_set_rps(dev_priv->dev, dev_priv->rps.idle_freq);
 		dev_priv->rps.last_adj = 0;
 		I915_WRITE(GEN6_PMINTRMSK,
 			   gen6_sanitize_rps_pm_mask(dev_priv, ~0));
